# Checks

```
python tests/run.py                                   # image maths + both frontends + the workflow
FORGE_ROOT=/path/to/forge-neo python tests/browser_smoke.py   # the real extension in a real browser
```

Nothing in `run.py` starts the WebUI. `tests/stubs/modules` stands in for the
handful of `modules.*` names the extension touches, `tests/stubs/modules_forge`
for Forge's canvas module (its markup, its hidden image textbox, and the
`ForgeCanvas` the host builds img2img with), and `tests/forge_like.py` builds
a page shaped like Forge Neo's `modules/ui.py` — the same component hooks, the
same constructor repair, the same two metaclass patches applied in the same
order (which is what makes a session rebuild of Forge's textbox lose its
`numpy=False`), the same canvases in img2img and Inpaint, an ImageStitch panel shaped like the
built-in script's (a hidden checkbox labelled with its title, an accordion,
the reference gallery with the script's id) in txt2img and img2img, and one
`gr.Tabs(elem_id="tabs")` around everything.

| file | what it holds the line on |
| --- | --- |
| `test_imaging.py` | layers composite at their offsets, clipped, hidden or faded, merge over each other, and give up the part of themselves inside a box or under a mask; a picture loads as Layer 1 over a white Background of its size; the document adds, moves, resizes (from the pixels a layer started with, about its centre, refusing a size no canvas holds), crops, expands and flattens layers, selects one or several (the primary being the last added), shows and hides, reorders, duplicates, deletes and merges the selection, and undoes all of it; the mask is coverage carried as the scribble layer's alpha and comes back unchanged; the high-contrast layer is the canvas's own checkerboard; an echo of a picture is recognised even after a browser's premultiplied re-encoding; the crop frame the browser reports is parsed, rounded and clamped; smoothing rounds an edge without eating a thin stroke; expansion places the original correctly, masks only what is new, carries an existing mask, refuses a canvas no browser can hold; the inpaint layer is what Forge's threshold reads; the document's undo history, original included; which destination a send picks |
| `test_frontends.py` | the Old UI setting exists, is saved, defaults to the touch Canvas and asks for a Reload UI; the mask style is the Inpaint tab's; each frontend mounts alone under the same tab; the surface is the host's markup with its two hidden textboxes and exactly one `load` event to attach it, told to fit the window; no sticky rule and no fixed rule outside focus mode; the shell is a work column (one action row of the menu button and the status, the canvas, then the hidden controls the menu presses) beside a rail of one panel per tool and no accordion; the menu reads the destinations this WebUI has, ImageStitch for both tabs; every picker in the rail is chips; the receive button lands in each output row right after "send to extras"; every event on the assembled page resolves; every step that replaces the image is image → wait → mask layer, and a step that left the canvas alone leaves the strokes alone; Undo and Redo try the canvas's stroke history first; a tap in the layer list, a drop, a resize and every panel action are the same view-keeping chain; the selection outline follows the preview browser-side; the canvas's image textbox has one input handler, filtered in the browser; painting, tools, aspect and the menu are browser-only; **no backend event writes a host image textbox**: a send request writes Extras and the ImageStitch galleries from the backend, the instruction and an image payload, and browser-only steps write the chosen host textbox (the other untouched), tick the ImageStitch box of the tab sent to, switch tabs, and after a wait on the Inpaint canvas write its mask; a Canvas that throws leaves a working legacy tab **and a working host**; a WebUI without ForgeCanvas picks legacy and says why |
| `test_workflow.py` | the session rebuild Gradio does after an update output, under Forge's metaclass patches: a rebuilt Canvas textbox still reads images, a rebuilt host textbox would not; receive lands as Layer 1 over a Background; an echo changing nothing; a picture opened on the canvas becoming the document; crop from a reported frame (with and without strokes) on both layers; undo, redo (strokes first, then steps); a tool asked for through the menu; Layers: the drag previews sent once per change, a selection becoming a layer without a reload, a drop moving the selection and nothing else and reloading the composite, picking, the box selecting several, re-centering, resizing (the slider, half, double, 100%, undo, a refused size), ordering, hiding, opacity, renaming, duplicating, deleting, merging (down, and several into one), flattening, a masked area becoming a layer, undo of each; a send request to Inpaint as an image payload then a mask payload, img2img dropping the mask, Extras and the ImageStitch galleries written directly (the picture saved where the host serves it from), the box each send ticks, Inpaint without a mask clearing the layer there; see-through pixels kept for img2img, Inpaint and ImageStitch, white for Extras, or filled by the setting; expand with auto-mask, clear, invert, reset, the side buttons and snapping, open from a file, save a copy |
| `browser_smoke.py` | every top-level tab switches *before* any Canvas feature is touched; the canvas attaches without an image and takes the height the window has left, the tab fits without scrolling, the rail is beside the canvas and no taller, nothing lies over the canvas; the action row is one menu button and the status; the menu lists Open, Edit, Tools, Panels, Focus and Send to, its Send to list every destination with the suggested one marked and a Cancel, its Tools list ticks the current tool, and Cancel, Back, Escape, a tap outside and an item each close it; Panels from the menu puts the rail away and back; the host's toolbar sits away from the frame's grip; receive from a gallery lands in the tab, fitted, as Layer 1 over a Background, with the frame over the whole image and the canvas outline on its edge, and the echo is not mistaken for an opened picture; a handle drag with the mouse and with a finger crops exactly the frame, the grip moves the frame without resizing it, and the frame resets to the result; crop → undo (from the menu) → crop again → undo; the aspect menu; one-finger pan and two-finger pinch, the outline following; the canvas's own fit button; a stroke, Undo taking it back (not the crop) and Redo, an erase, the tools and the size slider; Send to Inpaint from the menu arrives as image then same-size mask, drawn on that canvas, and the host switches to the Inpaint sub-tab; Expand adds 128px, masks it, and sends it see-through; Clear and Invert; Layers: the selection outlined, a selection becomes a layer without a reload, a mouse drag and a finger drag on the outline land the layer by the distance dragged with the zoom kept, a drag off the outline pans instead, undo, the list's taps (select, eye, box, arrows), a see-through canvas sent to img2img as it is, ×2 and 100% resizing with the outline and slider following, re-center, merge, delete; Send to ImageStitch from the menu fills that tab's reference gallery with the image alone, ticks its box and switches there, and a second send replaces rather than adds; Reset from the menu; a picture opened from Menu → Open (the file chooser of the hidden button) and one dropped on the canvas; Focus from the menu, Escape closing the menu first and focus mode next. **The whole flow runs again with WebGL disabled and must not differ.** Legacy mode: the iframe mounts alone. |

`browser_smoke.py` needs Playwright with a Chromium (`CHROMIUM=/path/to/chrome`
if it is not where Playwright keeps it) and `FORGE_ROOT` pointing at a Forge
Neo checkout: the page loads Forge's own `canvas.js`, `canvas.css`, `script.js`
and `ui.js`, because the Canvas is the host's canvas. The unit suites use the
real `canvas.html` from `FORGE_ROOT` when it is set and a minimal copy with the
same element ids otherwise.

`test_frontends.py` prints one traceback on purpose — it breaks the Canvas to
prove the fallback catches it.
