# Checks

```
pip install -r tests/requirements.txt                 # no torch, under two minutes
python -m playwright install chromium                 # for the browser suite in run.py
python tests/run.py                                   # image maths, both frontends, the workflow, WanGP, the queue, Clipboard, Canvas startup
FORGE_ROOT=/path/to/forge-neo python tests/browser_smoke.py   # the real extension in a real browser
```

`tests/run.py` says at the end how many of its suites actually ran. A suite
whose dependency is missing is skipped rather than failed, which is right for
a laptop and wrong for CI - set `MINIPAINT_REQUIRE_ALL_SUITES=1` to turn a
skip into a failure, as `.github/workflows/tests.yml` does.

Pin the whole Gradio ecosystem, not just Gradio. `tests/requirements.txt`
explains why in full: 4.40 declares `fastapi` and `pydantic` with no upper
bound and both have since moved past what it can work with, and neither
failure names its own cause.

## The Gradio the suite runs against

Pin Gradio to the version the target Forge ships, and check a change against
that one before shipping it:

```
pip install "gradio==4.40.0" "gradio-client==1.2.0" "huggingface_hub==0.24.6"
```

This is not housekeeping. Gradio's event graph behaves differently across major
versions, and a page that assembles cleanly on one can have a button that does
nothing on another. A change verified only against whatever Gradio happened to
be installed is a change verified against the wrong thing. `huggingface_hub` is
pinned because 4.40 imports a name that later releases removed.

To check a second version, build a venv for it rather than moving the system
install:

```
python3 -m venv --system-site-packages /tmp/venv-gradio6
/tmp/venv-gradio6/bin/pip install "gradio==6.27.0"
/tmp/venv-gradio6/bin/python tests/run.py
```

Nothing in `run.py` starts the WebUI. `tests/stubs/modules` stands in for the
handful of `modules.*` names the extension touches, `tests/stubs/modules_forge`
for Forge's canvas module (its markup, its hidden image textbox, and the
`ForgeCanvas` the host builds img2img with), and `tests/forge_like.py` builds
a page shaped like Forge Neo's `modules/ui.py` — the same component hooks, the
same constructor repair, the same two metaclass patches applied in the same
order (which is what makes a session rebuild of Forge's textbox lose its
`numpy=False`), the same canvases in img2img and Inpaint, an ImageStitch panel shaped like the
built-in script's (a hidden checkbox labelled with its title, an accordion,
the reference gallery with the script's id) in txt2img and img2img, and one
`gr.Tabs(elem_id="tabs")` around everything.

| file | what it holds the line on |
| --- | --- |
| `test_imaging.py` | layers composite at their offsets, clipped, hidden or faded, merge over each other, and give up the part of themselves inside a box or under a mask; a picture loads as Layer 1 over a white Background of its size; the document adds, moves, resizes (from the pixels a layer started with, about its centre, refusing a size no canvas holds), crops, expands and flattens layers, selects one or several (the primary being the last added), shows and hides, reorders, duplicates, deletes and merges the selection, and undoes all of it; the mask is coverage carried as the scribble layer's alpha and comes back unchanged; the high-contrast layer is the canvas's own checkerboard; an echo of a picture is recognised even after a browser's premultiplied re-encoding; the crop frame the browser reports is parsed, rounded and clamped; smoothing rounds an edge without eating a thin stroke; expansion places the original correctly, masks only what is new, carries an existing mask, refuses a canvas no browser can hold; the inpaint layer is what Forge's threshold reads; the document's undo history, kept as references so a checkpoint costs nothing; the drag preview carries the canvas size and the other layers' boxes, its picture only when the pixels changed (a move sends the box alone, the same underlay is not sent twice); transform mode's result (a box moved and made a width: scaled from the original pixels, a group about its box, capped at 40×, undone as one step); a picture added as a layer is fitted to the canvas (down or up), centred, from its own pixels, and asks for transform mode once; which destination a send picks |
| `test_frontends.py` | the Old UI setting exists, is saved, defaults to the touch Canvas and asks for a Reload UI; the mask style is the Inpaint tab's; each frontend mounts alone under the same tab; the surface is the host's markup with its two hidden textboxes and exactly one `load` event to attach it, told to fit the window; no sticky rule and no fixed rule outside focus mode; the shell is a work column (one action row of the menu button, the four tools as icon buttons, the Send to button and the status, the canvas, then the hidden controls the menu presses) beside a rail of one panel per tool and no accordion; the menu reads the destinations this WebUI has, ImageStitch for both tabs; every picker in the rail is chips; the receive button lands in each output row right after "send to extras"; every event on the assembled page resolves; every step that replaces the image is image → wait → mask layer, and a step that left the canvas alone leaves the strokes alone; Undo and Redo try the canvas's stroke history first; a tap in the layer list, a drop, a resize, transform mode's Done and every panel action are the same view-keeping chain that reads the strokes and never the picture; entering and leaving transform mode are browser-only and Done shows only then; Add image as layer is a hidden upload button the panel presses from the browser and a view-keeping layer step; Reselect is browser-only; each tool button is one backend event; the selection outline follows the preview browser-side; the canvas's image textbox has one input handler, filtered in the browser; painting, tools, aspect and the menu are browser-only; **a send names nothing of another tab's**: the send request writes this tab's own boxes only - the instruction, the image payload and a plan for placing it - and one browser step with no outputs at all places the picture, ticks the ImageStitch box, switches tabs and, after a wait on the Inpaint canvas, writes its mask; the ONE event in the whole page that names Extras or an ImageStitch gallery is the hidden button the browser presses when it could not place the picture itself, and how a send really ended comes back from the browser into the status line; the bar's Send to button opens the same list in the browser; a Canvas that throws leaves a working legacy tab **and a working host**; a WebUI without ForgeCanvas picks legacy and says why; the public queue API is loaded at startup right after the WanGP bundle, so its one snapshot at load tells the WanGP tab whether jobs use its settings |
| `test_workflow.py` | the session rebuild Gradio does after an update output, under Forge's metaclass patches: a rebuilt Canvas textbox still reads images, a rebuilt host textbox would not; receive lands as Layer 1 over a Background, the canvas getting a JPEG display copy (WebP when see-through); an echo changing nothing; a picture opened on the canvas becoming the document; crop from a reported frame (with and without strokes) on both layers; undo, redo (strokes first, then steps); a tool chosen from the bar; Layers: the drag previews sent once per change, a selection becoming a layer without a reload, a drop moving the selection and nothing else and reloading the composite, picking, the box selecting several, re-centering, resizing (the slider, half, double, 100%, undo, a refused size), transform mode's Done (placing and sizing, the same box again changing nothing, unreadable input ignored, a refused size undone), a picture file added as a layer (fitted, selected, Layers mode, the transform flag once, undo, a bad file, no picture), ordering, hiding, opacity, renaming, duplicating, deleting, merging (down, and several into one), flattening, a masked area becoming a layer, undo of each; a send request to Inpaint as an image payload then a mask payload, img2img dropping the mask, the plan each destination gets (the host canvas's own box, or the component to upload into with the ImageStitch box beside it and whether it is a gallery), the server placing a picture the page could not - once, for that destination only, saved where the host serves it from - and what the page reports back about a send that did not go cleanly, Inpaint without a mask clearing the layer there; see-through pixels kept for img2img, Inpaint and ImageStitch, white for Extras, or filled by the setting (which no longer offers gray); expand with auto-mask, clear, invert, reset, the side buttons and snapping, open from a file, save a copy |
| `test_wangp_queue.py` | the queue operation inside the bridge plugin, on a stub host with Wan2GP's own variable names (and, for protocol 5, `track`: waiting with a position, generating at the head of a running loop, finished only for a request this page saw queued and long after its admission record is pruned, unknown otherwise, refused for another page's session; and the `model_type` a request insists on, refused with `MODEL_CHANGED` before anything is written): an empty request writes only `client_id` and the trigger; a prompt goes into the box generation reads, wizard on or off; an image is a replacement, never an append, and one the live model cannot take is reported and left out; the trigger is never written when an image fails to stage, the id was used for another payload, or another request still owns the form; confirmation is queued on a task carrying the client id (and stays queued), refused only on a correlated `queue_errors` entry, expired to `ADMISSION_UNCONFIRMED` when the time is up, and the overrides go back only where the page still holds what the bridge wrote |
| `test_wangp_start.py` | protocol 4's start decision on the stub host: a build with `generate_trigger` offers start and says, live, whether WanGP is generating; `auto` on an idle WanGP writes the generate trigger and confirms `started` with a depth of 0 (and started sticks); `auto` while generating writes the add-to-queue trigger and confirms `queued` one behind; `never` takes the queue route whatever the flag says; a flag injected as a value rather than a function is unknown and degrades to the queue route with `start: "unknown"`; a build without the trigger says so and queues; the same id with another start mode is a conflict; the shared normalisers carry route, start, the flag and the depth and drop shapes they do not know |
| `test_interop.py` | the public API's server half: bytes behind a token (PNG, JPEG, WebP; text, an oversized picture and a lying content type refused), every shape of omitted, null and empty in a request, a staged token and a Clipboard asset turned into ordinary handoffs, a request with one bad handle refused whole and its files let go, the routes on a bare app behind the sign-in gate |
| `test_clipboard_store.py` | the library: a root chosen and proved usable, containment (a separator, a symlink, a moved file, an unminted id each refused with its own code), a Forge PNG kept byte for byte, pixels becoming a PNG, `name` and `name (2)`, rename keeping the id and the extension, delete leaving the history to say Missing, a file dropped in by hand appearing on Refresh, a root change moving nothing, the draft and the history read forgivingly and a broken document moved aside |
| `test_clipboard_outbox.py` | the server-owned queue outbox: a press while WanGP is not running is refused and stores nothing; a burst keeps its order with each job pending, unsent, owned by its page; one lease at a time across every page, a page's own jobs in order, a page that stopped asking not holding the others up; a report without the lease is nobody's; sent then done, with started or queued kept without the prompt; an expired lease returns an unsent job to pending and marks a sent one unconfirmed, which no later claim hands out; refused, unconfirmed and unreadable outcomes; cancel while pending and not while sending; retry as a new request for the page that pressed it; adopt; a WanGP restart marking what was in flight unconfirmed; history bookkeeping once; a flood refused as busy; old terminal jobs pruned; a broken document moved aside; the routes on a bare app, 409 while WanGP is not running; and where a job's WanGP settings came from, as a table of every case - flushed, recorded after no answer or a settings-load, factory inheriting and by choice, a page-run job, one not composed yet - with the press's own journal note |
| `test_clipboard_enhance.py` | enhanced prompts through ModelSwitchRefiner's MiniMax H3 writer, against a fake `mc_llm_api` shaped like its document: the loader finds the API off the extension's folder through the host's extension list and imports it with the folder appended to `sys.path`; capabilities and the tab's line (ready, model, blocked, unknown); the variant from the WanGP model type or architecture, none for anything else; FL2VA taking the first and last frame and dropping the reference, Ref2VA the first reference and dropping the frames, a reference alone for FL2VA dropped rather than captioned; a page on another model and a press without a typed prompt refused before anything is stored; the switch off by default and read from disk; an override saved with its provenance, kept on disk, restored; an enhanced press asking the LLM at once - the picture as an object, our origin, the saved override - and waiting as enhancing while a plain job pressed later on another page waits behind it; progress read on every read; a written prompt making the job pending with the typed one kept; failed, cancelled from LLM Studio, forgotten and over-long prompts ending the job honestly; cancel one (its LLM request too) and cancel everything (by origin, once); retry from the typed prompt, a written prompt carried over, and written again after `MODEL_CHANGED`; the owning page's tracking of a queued task with finished sticking; the routes (`/enhance`, submit with `enhance`, `/cancel_all`, `/track`); the tab's switch, editor, press, cards, history and Cancel everything; nothing in the log |
| `test_clipboard_ui.py` | the Clipboard tab on the Forge-shaped page: one tab under one id, a browser column twice the composer's width, every part a Gradio component, every event resolving, Add to Queue enabled before anything is composed while WanGP runs (and off, saying why, while it does not - a press then refused and nothing stored), an empty composer producing a job, a press appending a job to the server's outbox, the page claiming and reporting it, started and queued recorded in the history once and refusals and doubts not, Cancel, Retry and Run from this page, a cleared card changing nothing on WanGP, a request naming assets by id and never by path, a confirmed answer becoming a history record and a refusal not, Load filling the composer and queueing nothing, the intercept sending a gallery picture into Clipboard and passing it through when Clipboard cannot take it, Send to Clipboard from the Canvas, the way back through the Canvas's receive chain, a tab that cannot be built saying so with the host intact, the theming rules (theme variables only, no white, no fixed background), and where settings came from: Add to Queue carrying the page's save outcome onto the job, a Settings line on a composed job's card and none before, and Queue Send History keeping the key and showing the words |
| `test_clipboard_intercept.py` | Send to WanGP from the gallery, the server half: the intercept setting with its three destinations, the old on/off switch migrated and the settings route taking either; the tab graph carrying one more hidden box and naming no other tab's component; a gallery press per destination, the WanGP one freezing a transient behind a token that is in no library and distinct on every press; the roles from the page's inputs, and from the enhancer's mapping when the page has not said; describe; submit with and without inheritance, several roles at once, the ticked roles checked again at press time and refused with `INTERCEPT_NO_IMAGE_ROLE` when none survives, the enhance switch repaired, a rapid burst kept in order, a refusal storing nothing, cancel releasing the token and a server-run job adopting it; the saved system prompt override reaching the writer rather than the default; the history's cap, pin, delete, a load reconciled against the model and a broken document moved aside; the popup's history learning where a job's settings came from once it is composed and keeping it after the job has gone, and no other word; submit carrying the page's save outcome onto the job; the routes on a bare app; and the bundle's text |
| `test_intercept_browser.py` | the popup bundle in a Node DOM harness against a fake page: open on a handoff, and once only - the same handoff again is refused, because Gradio runs the chained browser step with the box's stale value after a failed receive; no role ticked refused in place; a refused submit kept open with its sentence; cancel releasing the token; a second picture replacing the first while the typed prompt and the ticks stay; facts from the page's model and inputs deciding the roles; the prompt and the enhance switch shared with the Clipboard tab's own boxes; Escape and Ctrl+Enter; history rows pinned first with Load, Pin and Delete; the dead-queue path staging over the route; and Generate asking the WanGP page to save its form first, once, before the submit, carrying the answer - still sending when no WanGP answers, within the popup's own limit, and sending nothing when the popup is cancelled during the wait |
| `test_queue_e2e.py` | a WanGP-shaped Gradio app with Wan2GP's variable names whose two triggers run a miniature of WanGP's chains (the generate one starting a run and raising the process-wide flag), the bridge placed by `insert_after`, driven through Gradio's predict endpoint with the test playing the browser: the live page started as it is when WanGP is idle, joining the run when it is not, `never` staging only; a staged image, a Clipboard asset and a prompt overlaid, queued with those pixels and put back; a retry a duplicate and a reused id a conflict; a field the text-only model cannot take ignored; one owner per page (`QUEUE_BUSY`); queued sticking after the queue empties; WanGP's own refusal read; nothing running the chain ending unconfirmed; a value the user edited meanwhile left alone; the wizard box when the wizard is on; the Clipboard composer at one end, the server's outbox (submit, claim, sent, done) in the middle and its history at the other; an enhanced press on an H3 model through the stand-in - the bridge's own model block choosing the variant, the written prompt reaching WanGP's task, `track` through the predict endpoint from waiting to generating to finished, and `MODEL_CHANGED` once the page moves; two pages two sessions; no prompt, filename or path in the log |
| `browser_loading.py` | the Canvas startup contract, over the real asset route with nothing inlined: a bundle delayed 400ms is not reported ready and has not attached while it is in flight, and attaches exactly once when it arrives; a WanGP 404 leaves the Canvas ready, attached and unbothered, and leaves no script element behind to be mistaken for success later; a Canvas bundle that 404s is a visible failed state with a Retry that makes a real second request and recovers without reloading the page, pressed both through the API and through the button the user is actually offered; an `attach` that throws is a failed startup rather than a promise that never settles; Reload UI's rebuilt surface (a new uuid) attaches, the old attachment is stood down, and the bundle is neither fetched nor executed twice; `load()` resolves to a boolean and is false when any required bundle failed; `ensure()` is idempotent under concurrent callers; Send to Mini Paint pressed before the bundle arrives waits for the adapter and still returns the backend's three inputs; the toolbar is centred on the work area within 4px at four widths and hugs its buttons; the crop grip keeps the midpoint unless the toolbar is genuinely in its way, then moves to the nearer free side and stays inside its own frame; a resize settles instead of fitting for ever; the canvas's paper is the theme's own block colour under a translucent neutral rather than the host's `#cccccc` daylight, following the theme in both directions and keeping Forge's 10px squares, with the host's plain-colour setting still deciding when it is on |
| `browser_clipboard.py` | the Clipboard tab in a page, including with Gradio's queue cut while plain HTTP is left alone - the shape of the reported failure - where a send to img2img still finishes over `/minipaint-clipboard/send` and a server-written destination says it cannot; tab switching stays exact with the tabs reversed, refuses to guess when a tab is not on the page, and falls back to counting only while every panel has a button; Add to Queue's press carries what came of saving WanGP's form first; the grid shows every picture as the same-sized tile whatever shape it is, the size slider actually resizes them (the value has to reach the grid, not the Gradio block around it, which Gradio gives the same class), a refresh does not snap them back, and a theme's own `button { min-width: fit-content !important }` (the Lobe theme's, verbatim, with every other way a stylesheet can size a button, a span, a picture or the grid) leaves every tile exactly its cell, its caption and its picture inside it - the script states the geometry inline, and this is where that is measured; a picture sends to img2img and to both ImageStitch galleries, each arriving, acknowledged by the server and confirmed in the status; a send made while the connection is down is reported as lost instead of vanishing, and sending works again once it is back; View Outputs plays a REAL clip (two seconds of VP8 at 24 fps, made with the ffmpeg Playwright installs beside its browsers - Playwright's Chromium has no H.264) on the page's own controls, every one 44px with the theme's button rule on the page: it plays, the time moves, loop is on by default and really loops, a press on the picture pauses and the clock stops, a frame each way is exactly one frame, the timeline seeks coarsely while held and exactly where it is let go (with a stand-in `fastSeek`, because Firefox has one and Chromium does not), loop, mute and speed are remembered for the next video, the keys work, a replaced video's late pause cannot stop its successor's clock, and closing the view stops everything; an output whose request left a recipe offers Load, one without says so, and Load puts the prompt and the first frame back into the request and closes the view; the tab fills the window - the browser column and the request column end at the bottom of it, a taller window gives the grid exactly the difference and the Prompt box the rest, left alone the fit writes nothing, and at 560px the columns stack and the grid keeps its old share |
| `browser_intercept.py` | the popup in a real page carrying Forge Neo's own gallery helper (which answers the inputs array, `[[item]]`): pointed at Clipboard, the gallery's 🖌️ button is still a framework event the server accepts and the picture lands in the library; pointed at WanGP, the page takes the button over - no queue submission at all - and opens the popup compact under the gallery with the picture the gallery shows frozen over the staging route, the composer's prompt, the roles, the WanGP dot and Generate; Generate queues one job with the start and end frames staged and the prompt typed, closes the popup, puts nothing in the library and one recipe in the popup's history, and says so in a toast; Escape queues nothing and releases the token; the menu offers the three destinations and choosing one round-trips through the settings route; with the queue dead the button still opens the popup, on a NEW token staged over the direct route, knowing which tab it came from |
| `browser_smoke.py` | every top-level tab switches *before* any Canvas feature is touched; the canvas attaches without an image and takes the height the window has left, the tab fits without scrolling, the rail is beside the canvas and no taller, nothing lies over the canvas; the action row is the menu button, the four tools as finger-sized icon buttons (the current one marked), the Send to button and the status; the menu lists Open, Edit, Panels, Focus and Send to, its Send to list every destination with the suggested one marked and a Cancel, its Edit list Undo, Redo, Reset and Save, and Cancel, Back, Escape, a tap outside and an item each close it; Panels from the menu puts the rail away and back; the host's toolbar sits away from the frame's grip; receive from a gallery lands in the tab, fitted, as Layer 1 over a Background, with nothing selected and the canvas outline on its edge, and the echo is not mistaken for an opened picture; a selection under 128 × 128 is dismissed with the Tiny Debounce notice, a drag draws the frame over what was dragged, Reselect clears it; a handle drag with the mouse and with a finger crops exactly the frame, the grip moves the frame without resizing it, and nothing is selected after the crop; crop → undo (from the menu) → draw → crop again → undo; the aspect menu reshapes a drawn frame in place and shapes the next drawn one, a user's frame survives a trip through another tool; one-finger pan and two-finger pinch, the outline following; the canvas's own fit button; a stroke, Undo taking it back (not the crop) and Redo, an erase, the tools and the size slider; Send to Inpaint from the menu arrives as image then same-size mask, drawn on that canvas, and the host switches to the Inpaint sub-tab; Expand adds 128px, masks it, and sends it see-through; Clear and Invert; Layers: the selection outlined, Reselect and a drag drawing a new selection instead of moving the layer, a selection becomes a layer without a reload, a mouse drag and a finger drag on the outline land the layer by the distance dragged with the zoom kept, a drag off the outline pans instead, undo, the list's taps (select, eye, box, arrows), a see-through canvas sent to img2img as it is, ×2 and 100% resizing with the outline and slider following, re-center, transform mode (the box replaces the frame and the panel shows only Done; a drag inside moves it by the distance dragged, a corner drag resizes it keeping its shape, the box snaps onto the canvas edge, Done applies it, undo takes it back), a plain drag snapping onto the top edge, merge, delete; Add image as layer through the file chooser lands the picture fitted to the canvas (which keeps its size), selected, with transform mode opened on it by itself, and leaving keeps it fitted; the bar's Send to button sits between the tools and the status, opens the destinations under itself and puts them away again; Send to ImageStitch from the menu fills that tab's reference gallery with the image alone, ticks its box and switches there, and a second send replaces rather than adds (the browser empties the gallery and hands it the file: a Gradio gallery that is holding a picture has no upload input until it is cleared); Send to Extras arrives and the tab is shown afterwards rather than before, whichever half placed it - this host's Extras image is an input to nothing, so Gradio gives it no upload input and that check is the server fallback's own; Reset from the menu; a picture opened from Menu → Open (the file chooser of the hidden button) and one dropped on the canvas; Focus from the menu, Escape closing the menu first and focus mode next; the canvas's paper is the theme's own block colour rather than the `#cccccc` daylight the real `canvas.css` paints, measured against that file rather than a quote of it. **The whole flow runs again with WebGL disabled and must not differ.** Legacy mode: the iframe mounts alone. |

### The WanGP page's heartbeat and saves, on a virtual clock

`test_wangp_protocol.py` drives the real WanGP bundle through Node against a
fake iframe whose bridge answers like the real one (a hello with its
capabilities, a press with the fingerprint as it was, a look with it as it is,
a commit landing a moment later), on a clock the harness advances: every timer
the bundle sets runs in order with the promises it releases settled between
one and the next, and `stall()` makes the page itself late. That is what lets
five-second beats, twenty-second silences, two-minute gaps between reloads and
one-second quiet periods be asserted exactly in a few seconds of real time.
The in-WanGP half - a PING answered without a click, only trusted events
counted as touches, throttled, and only for a parent that asked - is driven
separately against the real `bridge_js.document_script`.

### The four browser suites, and why there are four

`browser_loading.py` tests the Canvas *startup contract* - the timing between
the Gradio load callback, the dynamic `<script>`, the asset route, the script
executing, and `attach()`. It serves the real bundles through the real route
over HTTP, with nothing inlined, and makes them late, missing, broken or
already loaded. It stands ForgeCanvas in rather than using the real one,
because none of what it asserts depends on how well the canvas draws, and that
is what lets it run anywhere - including CI, which is the point.

`browser_clipboard.py` tests the *Clipboard tab* - sending a picture out to
each destination, and the thumbnail grid. Both had failed in the page while
`test_clipboard_ui.py`'s 181 checks passed, for the same reason as above:
a send is a hidden box plus a Gradio event crossing the queue, and the
thumbnail size is a CSS custom property resolved by a real layout. Neither is
visible to a graph test. It stubs the Canvas too, so it runs in CI.

`browser_intercept.py` tests the *Send to WanGP popup* - the gallery button
taken over by the page while the setting says WanGP, with no framework event
in the chain, and the same press with the queue cut. Its page carries Forge
Neo's own gallery helper in its real shape, because the first version of the
popup rode the button's Gradio event and that event was refused by Gradio
before its function ran, on a payload the helper's answer had been wrapped
into once too often: nothing was logged anywhere, and no graph test can see
that. It stubs the Canvas too, so it runs in CI. Its page carries the WanGP
tab as well (on its setup card, since the machine has no WanGP), for the check
that the tab's panel is *parked* rather than hidden while another tab is
selected: the panel's computed box under the real stylesheet, the width the
bundle kept for it, and an iframe under it receiving animation frames at the
rate of one under the tab on screen. Chromium ticks frames in a hidden frame
too, so the starvation Firefox shows cannot be reproduced there - only the
invariant the parking rests on, which is that the parked panel has its full
box where a hidden one has none. Taking the `display: block !important` out of
the parked rule fails exactly those checks. What the suite cannot say, the
host's journal did: Firefox also throttles a frame whose embedder is
`visibility: hidden`, so on that browser a parked page gets its geometry kept
and not its frames (`docs/wangp/PARKED_PANEL_2026-09-25.txt`, section 6).

`browser_smoke.py` tests the *editor*, and needs the real ForgeCanvas for
that, so it needs a Forge Neo checkout and does not run in CI.

The browser suites are run in their own interpreter by `run.py`. Building a
Forge-shaped page is not a pure function of the process - Gradio keeps a
render context, the host keeps captured components, the stubs keep their
callback lists - so a suite that builds one after twenty-two others gets a
page that will not build, and fails for a reason unrelated to what it tests.

They used to be one file, and that file inlined `minipaint_canvas.js` into the
page's head because it had no route to fetch it from. That single convenience
is how a Canvas that never attached shipped behind 180 passing checks:
`window.minipaintCanvas` was already there, so a bootstrap that awaited nothing
and attached nothing still found an adapter. Neither file inlines a bundle it
claims to load any more, and `browser_smoke.py` mounts the real route on the
demo's own app.

`browser_smoke.py` needs Playwright with a Chromium (`CHROMIUM=/path/to/chrome`
if it is not where Playwright keeps it) and `FORGE_ROOT` pointing at a Forge
Neo checkout: the page loads Forge's own `canvas.js`, `canvas.css`, `script.js`
and `ui.js`, because the Canvas is the host's canvas. The unit suites use the
real `canvas.html` from `FORGE_ROOT` when it is set and a minimal copy with the
same element ids otherwise.

`test_frontends.py` prints one traceback on purpose — it breaks the Canvas to
prove the fallback catches it. `test_wangp_receiver_contract.py` and
`test_clipboard_ui.py` do the same to the WanGP tab and the Clipboard tab.
